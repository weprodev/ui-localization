#!/usr/bin/env node

/**
 * CLI wrapper for sync-translations.js
 * This makes the script available as an executable command
 * when the package is installed.
 */

require('../scripts/sync-translations.js');
